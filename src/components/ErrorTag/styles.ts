import styled from 'styled-components'

export const ErrorTagContainer = styled.span`
  color: ${({ theme }) => theme['base-error']};
  font-size: 0.875rem;
  font-weight: 400;
  line-height: 130%;
`
